# This app runs fully locally and needs NO environment variables.
#
# All data is stored in your browser's localStorage (see lib/local-db.ts).
# There is no database, no API keys, and nothing to configure — just run:
#
#   npm install && npm run dev
#
# This file is intentionally left without any variables.
